// import { message } from "antd"

// const Message1 = props => {
//   if (props.type === "success") return message.success(props.msg)
//   else if (props.type === "error") return message.error(props.msg)
//   else if (props.type === "warning") return message.warning(props.msg)
//   else return message.info(props.msg)
// }

// export default Message1

import { message } from "antd"

export default message
